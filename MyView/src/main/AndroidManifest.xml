<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tt.myview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="ScreenAdapter.ScreenAdapter_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!--  ScreenAdapter.ScreenAdapter_Activity  -->
        <!--  EventDispatch.relationListView.RelationActivity  -->
        <!--  EventDispatch.listView_viewPager.SlideActivity  -->

        <!--  widget.SearchView_Activity"  -->


    </application>

</manifest>